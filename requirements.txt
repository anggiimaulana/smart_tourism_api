# requirements.txt — Smart Tourism Ciayumajakuning API
# Install: pip install -r requirements.txt
#
# Catatan: scikit-surprise diinstall TERPISAH karena butuh C compiler
#   pip install scikit-surprise

# ── Core FastAPI ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9

# ── Database ────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0            # driver async PostgreSQL
alembic==1.13.1            # migrasi schema (Sprint 3)
psycopg2-binary==2.9.9     # untuk script sync (seed.py, utils)

# ── Auth ─────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
passlib[bcrypt]==1.7.4

# ── Import Excel / Data ─────────────────────────────────────
pandas==2.2.2
openpyxl==3.1.2
numpy==1.26.4

# ── Sentiment Analysis ──────────────────────────────────────
transformers==4.41.0        # IndoBERT (HuggingFace)
torch==2.3.0                # CPU version — GPU tidak diperlukan untuk inference
scikit-learn==1.5.0
joblib==1.4.2

# ── Chatbot RAG (CPU-Friendly) ───────────────────────────────
google-generativeai==0.5.4  # Gemini API
groq>=0.9.0                 # Groq API (alternatif/fallback LLM)

# ── Recommendation ──────────────────────────────────────────
# scikit-surprise — install manual (lihat catatan di atas)

# ── Testing ─────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0               # async HTTP client untuk TestClient

# ── Utilities ───────────────────────────────────────────────
python-multipart==0.0.9